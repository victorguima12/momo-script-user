# YOLO model weights — 121MB, over GitHub's 100MB file limit.
# Downloaded automatically on first run by app/bootstrap.py (Dropbox).
panel.pt

# Logs / crash dumps
*.log
crash.log

# Python bytecode
__pycache__/
*.pyc

# Local work — projects and downloaded jobs never belong in the repo
*.mscript
*.mscript.bak

# PyInstaller build output
build/
dist/
*.spec
